import { CategoryPage } from "@/components/category-page";

export const metadata = { title: "Rare and Collectible" };

export default function RarePage() {
  return <CategoryPage slug="rare-collectible" />;
}
